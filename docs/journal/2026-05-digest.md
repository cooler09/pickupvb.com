# Journal digest — May 2026

Condensed roll-up of the **230 change-bundle entries** from May 2026 (2026-05-22 … 2026-05-31). Each section is one bundle: its title plus the **why** (from the entry's Context). The full, unabridged entries remain in git history — see the journal README's digest convention. Sections are anchored by the original file slug (e.g. `#bundle-32`), so citations that used to point at `2026-05-DD-<slug>.md` now resolve to `2026-05-digest.md#<slug>`.

> Generated mechanically from the original entries (faithful to the authors' Context wording, trimmed). For decisions/changes/patterns in full, recover the original file from git.

## 2026-05-22

### bundle-1

**Bundle 1 — billing typed errors, Next.js peer floor, force-dynamic cleanup (2026-05-22)** — First post-audit remediation sweep after the security/performance/architecture audits landed. Backfilled retroactively after Bundle 2 to give the journal a starting point.

### bundle-2

**Bundle 2 — React Compiler lint, focus trap, FieldError, postcss override (2026-05-22)** — Second remediation sweep, picking up the follow-ups left by Bundle 1. Four threads:

### bundle-3

**Bundle 3 — Registration price-unit enforcement (2026-05-22)** — [`docs/audits/registration-workflow.md`](../audits/registration-workflow.md) left two P1 items open:

### bundle-22

**Bundle 22: Test bootstrap close-out + Team aggregate coverage** — The architecture audit ([architecture.md](../audits/architecture.md)) listed "No automated test suite" as an open P1 with a 2026-05-22 status update still claiming "no Vitest config for `packages/domain` yet." That was stale. Reality on disk:

### bundle-23

**Bundle 23: events/[id] page diet via `loadEventDetail`** — Bundle 22 closed the last "easy" P1 on the architecture audit (test bootstrap). The only remaining architecture P1 was the [`events/[id]/page.tsx`](../../apps/web/src/app/events/%5Bid%5D/page.tsx) page diet — the file had grown from ~520 LOC at the 2026-05-17 audit to 887 LOC, mixing metadata…

### bundle-24

**Bundle 24: events/[id] JSX componentization (P2 follow-up)** — Bundle 23 extracted the data layer of `events/[id]/page.tsx` into a `loadEventDetail()` helper, dropping the page from 887 → 566 LOC. The audit's aspirational ≤300 LOC target wasn't reached because the remaining bulk was JSX render branches, not orchestration. That was explicitly carried as a P2…

### bundle-25

**Bundle 25: ISR detail-page Suspense (teams / groups / players)** — Bundle 13a (2026-05-24) made three of the four listing pages ISR-cacheable for anonymous traffic by introducing `createSupabaseAnonClient()` and moving viewer chrome into `'use client'` islands. The matching detail pages — `/teams/[id]`, `/groups/[id]`, `/players/[id]` — were explicitly deferred…

### bundle-26

**Bundle 26: `/events/[id]` viewer-independent cache layer** — Bundle 25 finished the ISR-shell refactor for three of the five P1 #1 detail pages: `/teams/[id]`, `/groups/[id]`, `/players/[id]`. The two deferred routes were `/events` and `/events/[id]`. Both interleave viewer-aware chrome (RSVP, waitlist, co-host controls, manage tools, host-payment side…

### bundle-27

**Bundle 27: CSP enforcement** — Bundle 15 (2026-05-24) added the full Content-Security-Policy header behind `Content-Security-Policy-Report-Only` to soak the allowlist without risking production breakage. The directives cover Cloudflare Turnstile (script + iframe), Supabase REST + Realtime (https + wss), OSM tiles, and the…

### bundle-28

**Bundle 28: real `test` scripts in the last 5 placeholder packages** — The organization audit's P1 #2 was "test scripts are no-ops; CI is green by accident." Bundles 22 (test bootstrap) and 23 (events/[id] page diet) brought Vitest online for `domain`, `application`, and `web` — those three packages have real `vitest run` scripts and real test suites. The remaining 5…

### bundle-29

**Bundle 29: pre-commit hooks, dep-pin cleanup, `--webpack` documented** — The organization audit's Bundle 28 closed P1 #2 (test scripts). The remaining backlog was a cluster of P2 items, several of which were small enough to bundle: pre-commit hooks (husky + lint-staged), inconsistent dependency pinning, and the long-standing "why is `apps/web` forcing `--webpack`?"…

### bundle-30

**Bundle 30: package READMEs** — The documentation audit's P1 #2 ("Missing package READMEs") has been sitting at 🟡 Partial since 2026-05-17, when only [packages/supabase/README.md](../../packages/supabase/README.md) landed out of the five missing. With the organization-audit P2 cluster cleared in Bundle 29, the…

### bundle-31

**Bundle 31: apps/web README** — Bundle 30 closed four of the five remaining package READMEs from the 2026-05-17 documentation audit's P1 #2 sweep. The web app was deferred because it needed a route-tree overview rather than a single layer-rules snapshot, which made it a different shape of doc than the per-package ones. This…

### bundle-32

**Bundle 32: ADR 0009 canonical-domain-apex + stale P2 cleanup** — After Bundle 31 closed the P1 README sweep, the documentation audit's remaining open items were: `CONTRIBUTING.md` (gated on the open-vs-closed-project question), the ADR backfill cluster, and a handful of P2s. Two of those P2s — "Server-action error-handling pattern is undocumented" and "Audits…

### bundle-33

**Bundle 33: ADR backfill — open-in-new-tab + Stripe webhook dedupe** — Bundle 32 closed the canonical-domain-apex ADR (landed at slot 0009 instead of the originally-suggested 0006 because the lower slots had been taken by `event-divisions` and the team-registration ADRs in the interim). Two backfills remained from the original 2026-05-17 documentation audit:

### bundle-34

**Bundle 34: testing strategy doc** — After Bundle 33 closed the ADR-backfill cluster, the P2 docs cluster was the next coherent backlog: api-reference, monitoring, testing strategy, JSDoc backfill, database-operations. Testing strategy is the highest-signal of the five — the original audit's premise ("no tests exist anywhere; new…

### bundle-35

**Bundle 35: API reference doc** — After Bundle 34 closed the testing-strategy P2, the remaining P2 docs cluster was api-reference / JSDoc-backfill / monitoring / DB-ops. api-reference is the highest-signal of the four because the README's 6-endpoint list at the time of the audit was both stale (missing 9 routes) and the only HTTP…

### bundle-36

**Bundle 36: monitoring doc** — After Bundle 35 closed the api-reference P2, three P2 docs items remained: monitoring, JSDoc backfill, DB-ops. Monitoring is the highest-signal of the three — there was no single document anyone could open during an incident to answer "where do I look?". The configuration itself is already in good…

### bundle-37

**Bundle 37: database operations doc** — After Bundle 36 closed monitoring, only two P2s remained on the docs audit: JSDoc backfill (gated on the architecture audit's bracket-generators P1 reclassification per the finding's own fix note) and the database operations guide. DB-ops is unblocked, so it's next.

### bundle-38

**Bundle 38: issue templates + CODEOWNERS** — After Bundle 37 drained the documentation audit's P2 docs (only JSDoc remains, gated), the natural next "doc-shaped" thing is the organization audit's long-running P2: **issue templates + CODEOWNERS**. The bug-report issue template + `config.yml` contact link had landed out-of-band and were already…

### bundle-39

**Bundle 39: migration preamble standard** — Bundle 38 closed the last open P2 on the organization audit (issue templates + CODEOWNERS) and Bundle 37 closed the last open P2 on the documentation audit (database-operations doc). The next-highest-signal doc item is the first P3 from the documentation audit: **migration preamble inconsistency**…

## 2026-05-23

### bundle-4

**Bundle 4 — Roster-mode per-team captain checkout (2026-05-23)** — [Bundle 3](#bundle-3) annotated the audit, shipped the ADR 0007 §3 validation + editable `team_registration_mode`, and left a detailed spec for the deferred Model P1: there was no Stripe path for a roster-mode captain to pay the per-team fee on a tournament with a `per_team`-priced…

### bundle-40

**Bundle 40: developer onboarding guide** — Bundle 39 closed the first P3 from the documentation audit (migration preamble standard). The next-highest-signal P3 is the **developer onboarding guide** — the audit asked for a small, opinionated "day 1" checklist that links into the other docs. CHANGELOG, flow diagrams, and code-of-conduct are…

### bundle-41

**Bundle 41: FieldError accessibility sweep complete** — Bundle 2 (2026-05-22) extracted the shared `FieldError` + `fieldA11y` primitive into [field-error.tsx](../../apps/web/src/components/field-error.tsx) and migrated two forms (`new-team-form`, `new-group-form`) as a proof-of-pattern. The accessibility audit's P2 finding tracked the remaining four…

### bundle-42

**Bundle 42: Accessibility quick-wins II** — After Bundle 41 closed the FieldError sweep, the accessibility audit's "Still open" list had several small, mechanical P2s that didn't merit individual bundles but did fit a clean quick-wins set. Three of them target component-level primitives where a single change benefits every caller, which made…

### bundle-43

**Bundle 43: Combobox ARIA on address + user pickers** — Two of the accessibility audit's remaining P2s targeted the same WAI-ARIA combobox pattern on two typeahead controls — the address autocomplete on event/community forms and the people-picker used for adding co-hosts, friends, and team members. The audit's open-questions section had even floated…

### bundle-44

**Bundle 44: Toast focus-ring contrast — last accessibility P2** — After Bundle 43 closed the combobox-pattern P2s, only one P2 was left in the accessibility audit: the toast close button's focus ring inherited its color from the toast foreground (`focus:ring-current`) which on info/warning variants didn't reliably hit the 3:1 contrast threshold that WCAG 2.4.11…

### bundle-45

**Bundle 45: Position-fill math into the event detail read model** — The architecture audit's "Still open" list carried a P2 (carried over from Bundle 23's loader extraction): the event detail page's loader still recomputed `filledByPosition` from `event.attendees` with a dedicated `for` loop, even though the infrastructure repository already walks attendees once to…

### bundle-46

**Bundle 46 — `co-host-actions.ts` flash-param redirects (2026-05-23)** — Carried over from the 2026-05-17 architecture audit, where the **Server-action error handling** pattern was documented in [AGENTS.md](../../AGENTS.md) and applied to `rsvp-actions.ts`, but [co-host-actions.ts](../../apps/web/src/app/events/%5Bid%5D/co-host-actions.ts) was left for a follow-up…

### bundle-47

**Bundle 47 — split `groups/actions.ts` into per-concern files (2026-05-23)** — The last remaining mechanical P2 from the 2026-05-17 architecture audit: [`apps/web/src/app/groups/actions.ts`](../../apps/web/src/app/groups/actions.ts) was a 166-LOC catch-all holding six unrelated server actions (group create/update, follow/unfollow, member add/remove/role-change). The sibling…

### bundle-48

**Bundle 48 — TODO/FIXME/HACK scan (close documentation-audit deliverable) (2026-05-23)** — The [2026-05-17 documentation audit](../audits/documentation.md) ended with a promised-but-unfinished deliverable:

### bundle-49

**Bundle 49 — Architecture P3 cluster closeout: JSDoc, hrefs, barrel-export docs (2026-05-23)** — After Bundle 48 closed the documentation-audit deliverable, the architecture audit's [Still open](../audits/architecture.md) list had two entries: the deferred **P2 mapper extraction** (blocked on a `group_members` DTO-shape design call) and the **P3 cluster** — "JSDoc on aggregate factories, hrefs…

### bundle-50

**Bundle 50 — Accessibility P3 cluster closeout (2026-05-23)** — After Bundle 49 brought the architecture audit down to its single deferred P2, the next-cleanest closeout target was the accessibility audit's three remaining P3 findings:

### bundle-51

**Bundle 51 — Documentation P2 JSDoc closeout (2026-05-23)** — Bundle 50 collapsed the accessibility audit to "all P3 resolved." The next-cleanest target was the documentation audit's lone open P2 — **JSDoc coverage gaps on core domain exports** — which had a self-referential gate: "once the bracket-generator P1 from the arch audit is reclassified." That gate…

### bundle-52

**Bundle 52: Team format mismatch reclassified as `ValidationError`** — The 2026-05-22 remediation log row in `registration-workflow.md` claimed that `RegisterTeamHandler` "now throws `ValidationError` when `division.format !== team.format`." The code that actually shipped threw `UnauthorizedError` for both the cross-event format check and the new cross-division check…

### bundle-53

**Bundle 53: Security P3 audit-text closure (#9, #10, #11)** — While picking the next audit item I cross-referenced the security audit's "## P3" section against the remediation log:

### bundle-54

**Bundle 54: SEO P3 closures + www → apex permanent redirect** — Continuing the SEO audit cleanup that Bundles 18, 20, and 21 started. Two P3 items had been deferred at "discussion-level" status: the `og:type` choice on event pages, and slug-based event URLs. Both needed a recorded decision so they'd stop resurfacing in audit scans. Separately, the SEO audit's…

### bundle-55

**Bundle 55: `RegisterTeamHandler` regression coverage** — Bundle 52 reclassified two `RegisterTeamHandler` throws from `UnauthorizedError` → `ValidationError` (the cross-event and cross-division format mismatch). That fix shipped without unit tests because the application package didn't have a test file for the team handlers — only…

### bundle-56

**Bundle 56 — 2026-05-23 — CONTRIBUTING.md (Documentation P1)** — The Documentation audit had one P1 left: "Missing CONTRIBUTING and LICENSE". The LICENSE half closed 2026-05-17 (MIT). The CONTRIBUTING half was gated on an open question that had been sitting unanswered for six days: \*\*"Is this an open-source / open-contribution project, or a personal / closed…

### bundle-57

**Bundle 57 — 2026-05-23 — CODE_OF_CONDUCT.md (Documentation P3)** — Bundle 56 shipped CONTRIBUTING.md with the explicit framing "PRs welcome but ad-hoc," which flipped the documentation audit's code-of-conduct gate from "skip — closed personal project" to "ship — accepting external contributions, however ad-hoc." Bundle 56 deferred the standalone CoC by inlining a…

### bundle-58

**Bundle 58 — 2026-05-23 — CI Supabase placeholder keys (Organization P3, Wontfix-by-design)** — The Organization audit's "CI uses hardcoded Supabase placeholder keys" P3 sat in an ambiguous state: the audit body said _"Either leave as-is (cheap and clear) or wire a real CI Supabase via the GitHub Action"_ — i.e. the audit itself proposed two acceptable answers and left the call open. The Open…

### bundle-59

**Bundle 59 — 2026-05-23 — TypeScript composite / project references (Organization P3, Wontfix-by-design)** — The Organization audit's P3 "TypeScript packages don't use `composite` / project references" was filed with an unusually self-aware remediation: _"Incremental builds would be slightly faster… Marginal at current scale. Revisit if build times become painful."_ Six months on, build times are still…

### bundle-60

**Bundle 60 — 2026-05-23 — CHANGELOG / changesets co-closure (Organization P3 + Documentation P3, Wontfix-gated-on-release-cadence)** — Two audits flagged the same missing artifact from two angles:

### bundle-61

**Bundle 61 — 2026-05-23 — Flow diagrams (Documentation P3, Wontfix-gated-on-complexity-growth) — Documentation audit fully closed** — Last P3 standing on the Documentation audit. The original framing was unusually permissive:

### bundle-62

**Bundle 62 — 2026-05-23 — Model P2 ad-hoc team registration: live-vs-dead audit (Registration workflow, ✅ Live verified)** — After Bundle 61 closed the Documentation audit, the Bundle 61 journal flagged that four consecutive Wontfix bundles had cleaned the P3 backlog and recommended shifting to a real implementation finding next. Scanning the still-open lists:

### bundle-63

**Bundle 63 — 2026-05-23 — Free-agent list groups by division on multi-division events (Registration workflow UX P3, partial close)** — Bundle 62 closed Model P2 ad-hoc live-vs-dead audit and recommended continuing on real-implementation findings rather than another Wontfix/verification pass. The Registration workflow audit's still-open list had three remaining UX P3 sub-items, the smallest of which was "Free-agent list still has…

### bundle-64

**Bundle 64 — 2026-05-23 — Friend-edges mapper extracted (Architecture P2 mapper extraction, partial close)** — Bundle 63 closed the small half of the Registration workflow UX P3 (free-agent list groups by division) and left the captain-claim affordance open. With that out of the way the next clean target on the audit backlog was the long-standing Architecture P2 \*\*"duplicated snake_case → camelCase row…

### bundle-65

**Bundle 65 — 2026-05-23 — Node 22 LTS bump (Organization P3, full close)** — Bundle 64 shipped the friend-edges sub-finding of the Architecture P2 mapper extraction and called out that the three originally-named row shapes (attendee, group-member, event-summary) all need either new render sites or a design call before they can be extracted — none of them is a clean next…

### bundle-66

**Bundle 66 — 2026-05-23 — Events page UX audit stale-note reconciliation** — After Bundle 65 closed the last P3 in the Organization audit (Node 22 LTS bump), the per-audit "open" list in [docs/audits/README.md](../audits/README.md) had no truly small open findings left. The remaining items are all either large (Performance ISR refactor, SEO slug-URL migration, Security P3…

### bundle-67

**Bundle 67 — 2026-05-23 — Remote Turbo cache (Organization P2, Wontfix-by-design)** — The Organization audit's P2 "No remote Turbo cache configured" was filed with the framing: _"Every CI run rebuilds from scratch; the Vercel free remote cache would meaningfully cut PR feedback time."_ That premise is worth testing against the current build, not inherited at face value.

### bundle-68

**Bundle 68 — 2026-05-23 — Model P1 `event_attendees.division_id` nullable: schema-enforcement bookkeeping close (Registration workflow, ✅ Live verified)** — After Bundle 67 closed the Organization audit's Bundle 65/66 follow-ups, the next implementation candidate up the queue was the Registration audit's open-play division picker (UX P1 half) — the only remaining P1-flavored item on the registration workflow board.

### bundle-71

**Bundle 71 — 2026-05-23 — Document Google OAuth + anon sign-in setup** — Two onboarding traps surfaced back-to-back while a new dev was setting up the stack:

## 2026-05-24

### bundle-5

**Bundle 5: free-agent division picker** — After Bundle 4 closed the last Model P1 on the [registration-workflow audit](../audits/registration-workflow.md), the highest-priority remaining item was the **UX P1 leftover** — free-agent and open-play signup paths had no division picker. The same surface also carried a Model P2 —…

### bundle-6

**Bundle 6 — Team-paradigm ADR (audit step 1 closeout)** — Bundle 5 ([journal](#bundle-5)) closed the last P1 leftover on the registration-workflow audit. The only doc-only blocker still open was **recommended sequencing step 1** — "decide on the team paradigm split (ad-hoc vs. roster) at the product level. _Write an ADR._"

### bundle-7

**Bundle 7 — Tournament tabs → single-flow picker** — Bundle 6 ([journal](#bundle-6)) ratified the team-paradigm doctrine in [ADR 0008](../adr/0008-team-registration-paradigm.md) and cleared the registration-workflow audit's recommended-sequencing step 1. The next item the audit called out (and the only remaining UX P2 on the registration…

### bundle-8

**Bundle 8 — Shared ESLint flat config across packages** — The cross-audit survey after Bundle 7 surfaced two open P1s on the [organization audit](../audits/organization.md):

### bundle-9

**Bundle 9 — Event detail page side-load parallelization** — After Bundle 8 closed the lint-coverage P1, the cross-audit survey pointed to the [performance audit](../audits/performance.md) as the next high-leverage P1. The audit lists four P1s for the event detail page:

### bundle-10

**Bundle 10 — Infrastructure `getDetail()` JOIN consolidation** — [Bundle 9](#bundle-9) closed the page-level half of [performance P1 #4](../audits/performance.md): the event detail page now issues at most two parallel side-load waves on top of `getEventDetail`. The infrastructure half — the `SupabaseEventRepository.getDetail()` aggregator itself…

### bundle-11

**Bundle 11 — Backend N+1 cleanup + Sentry CI build hotfix** — [Bundle 10](#bundle-10) closed the infrastructure half of [performance P1 #4](../audits/performance.md). The remaining open P1 in the performance audit is the listings-Suspense refactor (architectural, worth its own bundle). Before tackling that, this bundle clears three smaller P2/P3…

### bundle-12

**Bundle 12: Stripe dedupe / Photon timeout / OG cache** — Bundle 11 closed out the bigger backend N+1 work (push-subscriptions batching, `isPro()` / `isPlatformAdmin()` memoization) and shipped a CI hotfix for the Sentry-wrapped Next config. With those out of the way the performance audit's "Still open" list collapsed to:

### bundle-14

**Bundle 14: admin-client refactor (security P2 #4)** — After Bundle 13a closed out three of the four performance listings, we pivoted to the security audit. P2 #4 was the highest-impact open item that didn't require external infra decisions (CSP needs an allowlist inventory + report-only window; rate limiting needs a KV backend decision). It also reads…

### bundle-15

**Bundle 15: CSP Report-Only (security P2 #3)** — P2 #3 was last touched on 2026-05-17 when the five baseline headers (HSTS, `X-Content-Type-Options`, `Referrer-Policy`, `X-Frame-Options`, `Permissions-Policy`) shipped via `async headers()` in [apps/web/next.config.mjs](../../apps/web/next.config.mjs). The CSP piece was held back because it needed…

### bundle-16

**Bundle 16: rate limiting on email-sending paths (security P2 #6)** — P2 #6 in [docs/audits/security.md](../audits/security.md#6-no-rate-limiting-on-email-sending-paths) called out four files: the cron-driven notifications worker plus three user-facing server actions that hand off to Supabase Auth's `updateUser({ email })`, which in turn fires a confirmation email…

### bundle-17

**Bundle 17: FormData hard cap + Turnstile freshness** — Bundle 16 landed the last open security P2 (rate limiting on email-sending paths). The remaining security-audit backlog was P2 #3a (CSP enforcement promotion — needs a production soak window observing the Report-Only header shipped in Bundle 15 before flipping it on) and the P3 trifecta:

### bundle-18

**Bundle 18: BreadcrumbList JSON-LD on detail pages** — After shipping Bundles 14-17 across the security audit, the user asked to move to the next audit. The performance audit's remaining P1 #1 items (events-listing per-viewer Suspense refactor, detail-page overlay strategy) are too large for a single bundle. The architecture audit's P2 backlog…

### bundle-19

**Bundle 19: Architecture audit cleanup + teams page slug fix** — Bundle 18's scoping pass surfaced two architecture-audit P2 findings that were already resolved in the repository but still appeared "open" in the prose at the top of [docs/audits/architecture.md](../audits/architecture.md):

### bundle-20

**Bundle 20: SportsTeam + SportsOrganization JSON-LD** — The SEO audit's last open P3 was "Add `SportsTeam` / `SportsOrganization` JSON-LD on teams + groups detail pages." Bundle 18 had already shipped shared `BreadcrumbList` JSON-LD across all four detail pages; this bundle closes the matching entity-specific structured data on the two routes that have…

### bundle-21

**Bundle 21: Drop /teams/[slug] login gate** — Bundles 18 + 20 shipped `BreadcrumbList` + `SportsTeam` JSON-LD on `/teams/[slug]`, and the page has been in [sitemap.ts](../../apps/web/src/app/sitemap.ts) since Bundle 14. But the page handler still redirected unauthenticated visitors to `/login` — so Googlebot, OpenGraph crawlers, and Knowledge…

### bundle-69

**Bundle 69 — 2026-05-24 — ADR 0012: registration paradigm invariants (price unit × team mode × composition)** — Host feedback during a manual pass over the event-create flow surfaced that three knobs that read as independent in the form were actually entangled at the product level:

### bundle-70

**Bundle 70 — 2026-05-24 — Hide Pay CTAs when host can't actually collect** — Host-facing follow-up to bundles 69 (ADR 0012 invariants) and the subsequent UI cleanup that hid on-platform Stripe controls on the event-create form when `paymentsOffPlatform` was set. The mirror image on the public event page was still surfacing Pay buttons in two cases where the checkout…

### bundle-72

**Bundle 72 — 2026-05-24 — Off-platform UX: hide rosters, warn on external links** — Two related off-platform tournament UX papercuts surfaced while a host was setting up a tournament listing that links out to a third-party registration system:

### bundle-73

**Bundle 73 — 2026-05-24 — Community claims: defense-in-depth match check** — Audit follow-up to the community-listing claim flow shipped in bundle 72. The original `ClaimCommunityListingHandler` had a single authorization check:

### bundle-74

**Bundle 74: Community listing moderation queue (Stage A)** — Bundle 73 hardened community-listing claims with three layers (UI filter to eligible events, app-layer same-day + same-city match check, partial unique index on `claimed_event_id`). That removed the easy abuse path but still allowed a host who satisfied the match check to silently redirect a…

### bundle-75

**Bundle 75: PostHog analytics adapter + first server-side captures** — The repo had no first-party product analytics — only Vercel Analytics (page views + Web Vitals) and Sentry (errors). That left us blind on the questions that matter for both product iteration and ad/sponsor pitches: how many events are published per metro per week, what fraction of viewers RSVP…

### bundle-76

**Bundle 76: Complete server-side analytics capture set + PII guardrail test** — Bundle 75 shipped the `AnalyticsPort` scaffolding, PostHog adapter, and the first two captures (`event_published`, `event_joined`). The audit's P1 #2 taxonomy lists seven events; five remained unwired. This bundle finishes the server-side capture set and closes P1 #4 by adding a domain-layer Vitest…

### bundle-77

**Bundle 77: First-touch UTM attribution** — Bundle 76 closed every other P1 on the analytics audit. P1 #3 — UTM / attribution capture — was the last one blocking "spend a dollar on acquisition and know if it worked." Without it we can't answer "did the $500 Instagram test bring any signups?" with anything but a guess from Vercel Analytics…

### bundle-78

**Bundle 78: Public numbers page + marketable rollup views** — Bundle 77 closed the last P1 on the analytics audit. The user asked "what's the next hot integration?" and the recommendation was P2 #7 — the missing marketable / sponsorable public surface. Today the only external answer to "how big is PickupVB?" is "go read the events list and count" — useless…

### bundle-79

**Bundle 79: Web-vitals → product-analytics bridge** — Bundle 78 closed the marketable-surface P2. The next-cheapest item on the analytics audit was P2 #8: web-vitals (LCP / CLS / INP / FCP / TTFB) are captured by `@vercel/speed-insights` today but never reach PostHog, so we can't correlate "slow LCP" with the conversion events Bundle 76 wired…

### bundle-80

**Bundle 80: Analytics outbox at the handler boundary** — Bundle 79 closed P2 #8 (web-vitals → PostHog). The last architectural item on the analytics audit was P2 #6: "no server-side capture from domain events." Bundles 75 and 76 wired the typed `AnalyticsPort` and the first wave of captures, but every call site lived in the **action layer** — `joinEvent`…

### bundle-81

**Bundle 81: Consent affordance for analytics** — Bundle 80 closed the last architectural item on the analytics audit (server-side outbox at the handler boundary). The last open P2 was **#5 — anonymous-auth users have no consent affordance**: every visitor today is silently captured into Vercel Analytics and (since Bundle 75) PostHog. For a…

### bundle-82

**Bundle 82: Retire Vercel Analytics + Speed Insights pre-launch** — Bundle 81 closed the last P2 on the analytics audit (P2 #5 — consent affordance). The remaining items were all P3. P3 #12 — "Vercel Analytics + PostHog overlap to retire" — was authored with a post-cutover 30-day parity window in mind, on the assumption we'd already have production traffic in…

### bundle-83

**Bundle 83: Pass Stripe processing fee to the buyer** — The monetization audit landed earlier today. P1 #2 was the cheapest, highest-leverage finding in the backlog: today, when a host runs a $20 paid event in buyer-paid mode, they receive **$19.09** — Stripe's 2.9% + 30¢ processing fee comes out of the destination charge silently. The 5% PickupVB…

### bundle-84

**Bundle 84: Host-owned sponsor slot v1 (Pro-only core)** — After Bundle 83 closed monetization P1 #2 (buyer-paid processing fee), the next sequenced item from the monetization audit was P2 #4: **host-owned sponsor slot**. The key product decision was already made in the audit and user follow-up: PickupVB should not become a third-party ad seller; instead…

### bundle-86

**Bundle 86: Saved event templates (Pro host feature)** — Monetization Bundle 85 closed the sponsor-slot follow-up (P2 #4). The next sequenced monetization item was P1 #1 sub-item #1 from [docs/audits/monetization.md](../audits/monetization.md): **saved event templates** to increase Pro value for serial hosts below the fee-savings break-even.

### bundle-87

**Bundle 87: Host analytics dashboard (Pro feature)** — After Bundle 86 (saved templates), the next monetization-sequenced item from [docs/audits/monetization.md](../audits/monetization.md) was P1 #1 sub-item #2: **host analytics dashboard**.

### bundle-88

**Bundle 88: Opas audit remediation for Bundles 84–87** — The monetization audit's remediation log (2026-05-24) called for an Opas audit of Bundles 84–87 before resuming higher-impact monetization features. This bundle is the result of that audit: five fixes across the sponsor-slot and host-analytics surfaces.

### bundle-89

**Bundle 89: PII audit P1 remediation — soft-delete, FK nullability, profiles RLS** — Three migrations implementing the P1 findings from [docs/audits/privacy.md](../audits/privacy.md). These are the structural prerequisites for a future "Delete my account" feature — without them, deleting a user's `auth.users` row either (a) fails due to `ON DELETE RESTRICT` on `events.host_id` /…

### bundle-90

**Bundle 90 — Hero Images** — **Date:** 2026-05-24 **Scope:** Events, groups, and public profiles — wide banner image upload and display.

### bundle-13a

**Bundle 13a: ISR listings-Suspense (players / groups / teams)** — After Bundle 12 closed out the remaining P2 / P3 small-wins, the performance audit's only architectural item was P1 #1 — `force-dynamic` removed but the public listings still implicitly dynamic via `cookies()` in the RSC body. The audit's prescription:

### hero-images-proposal

**Hero Images Proposal (2026-05-24)** — - User requested an evaluation / write-up for adding hero images to events, groups, and public profiles. - The repo currently only has URL-based `avatar_url` fields for these surfaces; there is no existing upload flow to extend. - The ask was to save the analysis into the audit docs so it can be…

## 2026-05-25

### bundle-91

**Bundle 91: e2e test plan gap-fill + flake fixes** — Stabilize the e2e suite against dev: fix known-flaky tests, audit [docs/e2e-test-plan.md](../e2e-test-plan.md) for coverage gaps, add tests where gaps exist, and re-run the verify gate clean.

## 2026-05-26

### bundle-92

**Bundle 92: data-lifecycle audit remediation (retention cron + e2e cleanup helper)** — Implement the cheap, high-value findings from [docs/audits/data-lifecycle.md](../audits/data-lifecycle.md) so the remaining open items in that audit are real product work (group/team delete UX, ad-hoc soft-delete, broadcasts hide) rather than infra debt.

### bundle-93

**Bundle 93: group + team delete UX, broadcasts hide schema** — Second slice of the data-lifecycle audit ([docs/audits/data-lifecycle.md](../audits/data-lifecycle.md)). Bundle 92 handled the cron infra; this bundle takes the M-effort UX items the audit had been deferring:

### bundle-94

**Bundle 94: ad-hoc team registration soft-delete** — The only delete path for an ad-hoc team registration today is `hostForceWithdrawTeamRegistration` in [apps/web/src/app/events/[id]/host-team-registration-actions.ts](../../apps/web/src/app/events/[id]/host-team-registration-actions.ts). It already gates on `payment_status ∈ {None, Refunded}` — if…

### bundle-95

**Bundle 95: e2e cheap-wins (unblock 5 fixmes)** — `apps/web/tests/e2e/event-host.authed.spec.ts` had the only working event-creation recipe in the suite — packed inline in a 70-line `beforeAll`. It encoded several non-obvious things (DateTimePicker trigger selector vs hidden ISO input; address-disclosure collapse on `#addressLine` fill…

### bundle-96

**Bundle 96: Stripe e2e harness + paid-flow fixme graduation** — Step 2 of the e2e roadmap from bundle 95's hand-off note: build a Stripe test-mode harness so the ~14 paid-flow `test.fixme` stubs can become actual coverage. Stripe is the single biggest unlock — `test.fixme`s in `event-attendance`, `event-create-extended`, `events`, and `billing-stripe` all…

## 2026-05-27

### bundle-97

**Bundle 97 — e2e fixme triage follow-up to bundle 96** — After bundle 96 graduated the 5 paid-flow + tip Stripe e2e tests, 18 `test.fixme` markers remained across `apps/web/tests/e2e/`. The user asked: "can we continue with the other e2e tests that are skipped?"

### bundle-98

**Bundle 98 — monetization P1/P2 closeout: refund-policy gating, trial-funnel analytics, ADR** — The [monetization audit](../audits/monetization.md) had three remaining un-shipped items from its "What I'd ship in what order" sequence after Bundle 87:

### bundle-99

**Bundle 99 — invite-only / private events (P1 #1 closeout)** — [Monetization audit](../audits/monetization.md) P1 #1 had one open sub-item after Bundle 98:

### bundle-100

**Bundle 100 — off-platform event upsell (P2 #7)** — [Monetization audit](../audits/monetization.md) P2 #7 was the last remaining "ship it" item that wasn't either already shipped or deferred-on-trigger (P2 #6, metro sponsorship, is waiting on a real anchor advertiser). The hole the audit named:

### bundle-101

**Bundle 101 — fix PostHog "no events arriving" on prod** — User report: "my posthog analytics aren't working." Symptom narrowed to zero events arriving in the PostHog project on prod, despite `POSTHOG_API_KEY` + `POSTHOG_DISTINCT_ID_SALT` being set in Vercel and the composition root resolving to `PostHogAnalytics` (not the noop).

### bundle-102

**Bundle 102 — ship PostHog browser SDK (and the consent / docs pivot it requires)** — Bundle 101 fixed the **server-side** PostHog flush race. With that fix landed, the audit's diagnostic question came back: why does PostHog show zero visitors and zero page views? Static answer: because we have no browser instrumentation. Server-side `capture()` only fires on requests that reach our…

### bundle-103

**Bundle 103 — Live score tracker (first host tool)** — The host-tools landing at `/tools` has been a static "Coming soon" grid since launch. The user kicked off the host-tools tier with two questions: should these be Pro, and can we start with a live score tracker that covers both pickup ("disposable scoreboard on the gym TV") and tournament ("save the…

### bundle-104

**Bundle 104 — Bracket: walk-in / unregistered team escape hatch** — User asked to "implement a phase 1 generic tournament bracket creator." Surface read of the request looked like greenfield work, but a quick spelunk found the feature is already substantially shipped: 891 LOC under `apps/web/src/app/events/[id]/bracket/`, a full…

### bundle-105

**Bundle 105 — Bracket: realtime updates for spectators** — Picking up the next bracket item after bundle 104 (walk-in team escape hatch). User chose "live updates: when the host enters a score on one device, every viewer's bracket page refreshes without manual reload." Today the page is a server component that only re-renders when the submitter's own…

### bundle-106

**Bundle 106 — Bracket: drag-to-reorder seeding** — Continuing the bracket sweep. Today the seeding list is a static `<ol>` — order is fixed by whatever the server returns; the host's only reorder tools are the **Randomize** button (server-side shuffle) or hand-editing the URL. Hosts who want a specific seed order (e.g. "this is the #1 seed because…

### bundle-107

**Bundle 107 — Bracket: tree-style visualization** — Previous bracket layout rendered each round as a fixed-width column of stacked match cards with no visual relationship between a match and its two feeders. Functional, but the bracket didn't _look_ like a bracket — readers had to mentally trace seed numbers to figure out who plays whom in round 2.

### bundle-108

**Bundle 108 — Bracket: public spectator view** — Pre-this-bundle, the only way to watch a tournament bracket update was to open `/events/[id]/bracket` — a page designed for hosts and team captains. It carries reset-bracket controls, generate-playoff forms, edit-result detail panels per match, host notice flashes, and `export const dynamic =…

### bundle-109

**Bundle 109 — Bracket: format picker UX** — The bracket creation form was a bare `<select>` with four labels: "Single elimination", "Double elimination", "Round robin", "Pool play → playoff". A host who'd never run a tournament had no way to choose other than guessing or leaving the page to research. The pool-play extras (pool count…

### bundle-110

**Bundle 110 — Walk-in team form: capture roster inline** — The phase-1 walk-in / ad-hoc team form was deliberately name-only: a host running a bracket at the venue could type "Court 3 Spikers" and get a team into the bracket immediately, but to record who was actually on that team they had to leave the bracket page and edit the team elsewhere. In practice…

### bundle-111

**Bundle 111 — Bracket: one-click spectator link share** — The watch route shipped in bundle 108 with a single discovery path: a "Open public spectator view →" anchor on the host bracket page. To actually share that URL with players or spectators a host had to open the watch page, copy from the address bar, paste into a group chat — three context switches…

### bundle-112

**Bundle 112 — Bracket: "Jump to latest match" affordance** — A 24-team double-elim bracket renders 40+ cards across winners, losers, and grand-final columns. On a phone the page is a long vertical scroll; on the projector the host wants the live match front and centre, not "scroll down past the round-1 outcomes to find what's playing right now." The realtime…

### bundle-113

**Bundle 113 — Bracket: auto-scroll + flash on realtime update** — Bundle 112 gave the spectator a manual "Jump to latest" anchor. The realtime refresher (108) already triggers a re-render every time a match result lands — but the cursor didn't move, so the spectator still had to notice the bracket updated and tap the button to follow play. On a phone in a noisy…

### bundle-114

**Bundle 114: Bracket spectator OG image** — Bundle 111 shipped one-click copy of the spectator URL. As soon as people start sharing those links in group chats and on social, the unfurled preview matters — and right now the bracket-watch route inherits no OG metadata of its own. It would fall back to the site-level OG image (or nothing)…

### bundle-115

**Bundle 115 — Bracket follow-ups sweep** — Closing out the menu surfaced at the end of [bundle 114](#bundle-114): seven small bracket follow-ups grouped into one bundle so the watch page, format picker, and OG cards reach feature-parity with the rest of the events surface.

### bundle-116

**Bundle 116 — Promote ad-hoc registrations to first-class teams (2026-05-27)** — User-reported regression after bundle 115 walk-in form work: "i registered some ad hoc teams but they don't seem to show up on the event page or in the bracket page. It still shows 0." Traced to a structural gap that bundle 104's journal had asserted away ("the new team lands in the seeding list on…

### bundle-118

**Bundle 118 — Division-centric registration: multi-division free agents, host opt-out, cross-event guard (2026-05-27)** — Bundle 117 (the registration-workflow re-audit) was triggered by the product reframing "the event is a container; each division owns its teams + free agents." The re-audit logged six new findings; this bundle closes three of them in one migration:

### bundle-119

**Bundle 119 — Per-division team registration mode (ADR 0016) (2026-05-27)** — Bundle 117 (registration-workflow re-audit) logged six findings; Bundle 118 closed R1 / R2 / R6. This bundle closes **R3 (P2)** — `team_registration_mode` lived on `events`, which forced hosts running a multi-division tournament to pick one mode for the whole event. The concrete pain the user…

### bundle-120

**Bundle 120 — Walk-in team registrations (ADR 0017) (2026-05-27)** — Bundle 117 (registration-workflow re-audit) logged six findings. Bundles 118–119 closed R1 / R2 / R3 / R6, leaving **R4 (P3)** as the last open item: walk-in team registrations. The pain point is concrete and recurring at indoor tournaments — a captain shows up the day of the event with a roster…

### bundle-121

**Bundle 121 — Free-division price-unit exemption (amends ADR 0012) (2026-05-27)** — A host hit the create-event boundary with a free (price 0) division configured as ad-hoc + per-player and got [`Team-registered divisions require per-team pricing…`](../../packages/domain/src/events/volleyball-event.ts). The error is correct under ADR 0012's stated rule (Rule 2 — team-led divisions…

### bundle-122

**Bundle 122 — Pool play configuration: bestOf + fixed-games schedule (Phase 1 of ADR 0018) (2026-05-27)** — A host running a same-day pool-play tournament asked for more control over the `pool_play_playoff` format: match length (best of 1 vs. 3), a "games per team" cap so a 6-team pool doesn't run all 15 matches, a designated work/ref team per match, and "no team in two places at once" so the schedule…

### bundle-123

**Bundle 123 — Pool play: work/ref team per match (Phase 2 of ADR 0018) (2026-05-27)** — Bundle 122 shipped Phase 1 of [ADR 0018](../adr/0018-pool-play-configuration.md) — `bestOf ∈ {1,3,5}` and the `fixed_games` schedule mode. Phase 2 covers the work/ref team requirement: in pools with an **odd** team count there's always exactly one team sitting out each round, and hosts want that…

### bundle-124

**Bundle 124 — Pool play: courts + parallel-slot solver (Phase 3 of ADR 0018) (2026-05-27)** — Bundles 122 (Phase 1) and 123 (Phase 2) shipped match-length, schedule density, and the work / ref team for [ADR 0018](../adr/0018-pool-play-configuration.md). Phase 3 closes the original ask: hosts running a same-day tournament on multiple courts need each match tagged with **which court** it…

### bundle-125

**Bundle 125 — Pool play: host-driven match reorder (Phase 1b of ADR 0018) (2026-05-27)** — Bundles 122–124 shipped the bulk of [ADR 0018](../adr/0018-pool-play-configuration.md): bestOf + schedule density (Phase 1), work-team assignment (Phase 2), and courts + parallel-slot solver (Phase 3). The generator picks a deterministic round-robin order, but real-world hosts always want to nudge…

### bundle-126

**Bundle 126 — Pool play: per-pool courts (2026-05-27)** — Bundle 124 shipped a bracket-wide `courtLabels` list + greedy slot solver. Multi-pool tournaments often run on physically separate sets of courts — Pool A on the east wall, Pool B on the west — and want the schedule to reflect that. With the previous model every pool drew from the same flat court…

### bundle-127

**Bundle 127 — Bracket creator polish + site-wide CTA vocabulary (2026-05-27)** — User started with "improve the UX on the bracket creator: move important stuff up and emphasise CTAs." Shipped that, then asked "any other areas on the site that can use UX updates?" — quick scan surfaced a forked primary-CTA vocabulary, several muted-text empty states with no actual button, and…

## 2026-05-28

### bundle-128

**Bundle 128 — FormModal primitive + walk-in team modal conversion (2026-05-28)** — User asked to evaluate where modals would beat inline `<details>` for data isolation. Quick scan (via Explore subagent) found ~12 `<details>` usages, 6 custom toggle patterns, and 8 server-action forms behind toggles. The walk-in team form was the highest-leverage modal candidate — appears in three…

### bundle-129

**Bundle 129 — M3 alignment tokens (2026-05-28)** — Follows the [m3-alignment audit](../audits/m3-alignment.md) landed earlier the same day, which evaluated adopting Material Design 3 in response to repeated mobile-UX rework. Recommendation B + C: adopt M3 as a **design system** (tokens + principles) layered on Tailwind v4, with Radix UI for the…

### bundle-130

**Bundle 130 (M3 Touch Targets)** — Audit P1 #3 documented that despite [accessibility.md](../audits/accessibility.md) having previously bumped the mobile-menu hamburger and notification bell to 44 × 44, drift had crept back across icon-only buttons elsewhere: toast close `×`, modal close `×`, pagination prev/next, co-host remove…

### bundle-131

**Bundle 131 (M3 State Layers + Button Vocabulary)** — Audit P2 #4 noted that hover/focus/pressed handling is bespoke per component: `PrimaryButton` used `hover:opacity-90`, `MobileMenu` used `hover:bg-fg/5`, `Pagination` used whatever was easiest at the call site. M3 standardizes this as a **state layer** — a single `currentColor` overlay at a…

### bundle-132

**Bundle 132 (Radix Toast)** — Audit P2 #8 called out that [toast.tsx](../../apps/web/src/components/toast.tsx) stacked unbounded toasts in the top-right, exposed no action affordance, and used a single hard-coded `durationMs` default. M3 Snackbar prescribes the opposite on every axis: single visible snackbar with the rest…

### bundle-133

**Bundle 133 (BottomNav + FAB primitive)** — Audit P2 #11 called this out as "the single biggest mobile UX win on the table." The site shipped a desktop top nav that collapses on mobile to a hamburger which hides every primary destination (Events / Groups / Teams / Profile). M3 explicitly reserves the hamburger for **secondary** destinations…

### bundle-134

**Bundle 134 (Dialog + BottomSheet on Radix)** — Audit P2 #9 called out that the Bundle 128 `<FormModal>` is a solid native `<dialog>` shell but missing the M3 dialog structure: no icon slot, no spec'd action ordering, no full-screen-on-compact variant. Audit P2 #14 called out the related mobile UX miss: centering a `max-w-md` card on every…

### bundle-135

**Bundle 135 — TextField primitive (2026-05-28)** — [P2 #13](../audits/m3-alignment.md#13-text-fields-lack-m3-structure-filled-vs-outlined-supporting-text-leading-icon) flagged that every form in `apps/web/src/app/**/*-form.tsx` open-codes the same `<label>` + `<input className={inputClass}>` + `<FieldError>` trio, with the field-error wiring…

### bundle-136

**Bundle 136 — Dropdown menu on Radix (2026-05-28)** — [P2 #12](../audits/m3-alignment.md#12-details--popover-patterns-used-where-m3-prescribes-menu) flagged that the header's `<NavDropdown>` hand-rolled the focus management Radix would have given us for free — Escape-to-close, click-outside dismissal, route-change close — but skipped the features…

### bundle-137

**Bundle 137 — Density scale (2026-05-28)** — [P2 #15](../audits/m3-alignment.md#15-no-density--responsive-density) flagged that every list, table, card uses one set of paddings — specifically receipts and earnings tables cram on mobile while group-member lists are sparse on desktop. The audit prescribed the M3 density scale (`comfortable` /…

### bundle-138

**Bundle 138: system theme mode (M3 P3 #19, final M3 bundle)** — [m3-alignment.md](../audits/m3-alignment.md) flagged ten bundles across P1/P2/P3 for M3 adoption. Bundles 129–137 (change-bundle numbers; audit Bundles 1–9) closed the P1 + P2 work plus the density scale. This bundle (audit Bundle 10) was the audit's opportunistic "P3 cleanup" slot. The session…

## 2026-05-29

### bundle-host-social-handles-read

**Bundle: host social-handles read (Phase 2b remnant)** — Closes the one profile read deferred since Phase 2b inc. 7 — the primary host's social handles on the event detail page. Small, but it also clears a latent bug.

### bundle-phase-0-architecture-guardrails

**Bundle: Phase 0 architecture guardrails (brand smart constructors + Onion-layer lint ratchets)** — First phase of the architecture refactor scoped in the [2026-05-29 architecture re-audit](../audits/architecture.md#reevaluation--2026-05-29). Phase 0 is the "guardrails first" step: mechanical, low-risk changes that create the safety rails the later structural phases (ProfileRepository…

### bundle-phase-1-division-scoped-entries

**Bundle: Phase 1 — division-scoped team & free-agent entries (P1)** — Phase 1 of the architecture refactor, closing the **P1** from the [2026-05-29 re-audit](../audits/architecture.md#reevaluation--2026-05-29): team and free-agent registrations were persisted through a path that **sidestepped the `VolleyballEvent` aggregate**. The aggregate couldn't represent which…

### bundle-phase-2a-social-graph-port

**Bundle: Phase 2a — SocialGraphQueries port (shrink the EventRepository god-port)** — First increment of Phase 2 from the [architecture re-audit](../audits/architecture.md#reevaluation--2026-05-29). Phase 2 as scoped is large (2–3 days: a `ProfileRepository` for the orphan `UserProfile` aggregate **plus** draining ~38 raw `profiles`/`friendships` queries out of the web layer…

### bundle-phase-2b-inc10-friend-writes

**Bundle: Phase 2b (increment 10) — friend writes** — The last profile-write piece of the P2-1 drain. inc. 8–9 migrated the `profiles`-column writes behind the `UserProfile` aggregate; this migrates the `friendships` edge writes — a deliberately **different shape** (an edge table, not a profile column), per [ADR…

### bundle-phase-2b-inc2-players-directory

**Bundle: Phase 2b (increment 2) — players directory behind ProfileQueries** — Continues the P2-1 web-layer DB drain ([architecture audit](../audits/architecture.md#reevaluation--2026-05-29)). [Increment 1](#bundle-phase-2b-profile-queries-foundation) stood up the `ProfileQueries` read port + adapter and migrated `searchPeople`. This increment grows the port and…

### bundle-phase-2b-inc3-player-profile-page

**Bundle: Phase 2b (increment 3) — player profile page behind ProfileQueries** — Continues the P2-1 web-layer DB drain ([architecture audit](../audits/architecture.md#reevaluation--2026-05-29)). Increments [1](#bundle-phase-2b-profile-queries-foundation) and [2](#bundle-phase-2b-inc2-players-directory) drained `searchPeople` and the players directory…

### bundle-phase-2b-inc4-og-fix-and-batch-reads

**Bundle: Phase 2b (increment 4) — OG image fix + findCardsByIds batch drain** — Two related changes requested together; each is independently committable.

### bundle-phase-2b-inc5-followups

**Bundle: Phase 2b (increment 5) — follow-ups (escapeLike test + findCardById)** — Cleared the tracked follow-ups from earlier increments.

### bundle-phase-2b-inc6-friend-edges-port

**Bundle: Phase 2b (increment 6) — friend-edges read behind SocialGraphQueries** — Continues the P2-1 drain into the `friendships` reads.

### bundle-phase-2b-inc7-event-loader-captain-read

**Bundle: Phase 2b (increment 7) — event-loader captain read behind ProfileQueries** — Small, bounded drain of the previously-deferred `load-event-detail.ts` reads.

### bundle-phase-2b-inc8-user-profile-write-aggregate

**Bundle: Phase 2b (increment 8) — UserProfile write aggregate** — The first **write** increment of the P2-1 drain. Reads were nearly fully drained (inc. 1–7); this stands up the aggregate + repository the profile writes route through. ADR-worthy, so it ships with [ADR 0020](../adr/0020-user-profile-write-aggregate.md).

### bundle-phase-2b-inc9-theme-hero-business-writes

**Bundle: Phase 2b (increment 9) — theme / hero / business writes** — Continues the P2-1 profile-write drain. inc. 8 stood up the `UserProfile` write aggregate + `SupabaseUserRepository` + `getUserProfileHandlers()`; this increment drains the three remaining **profile-column** writes through that same seam, per the [ADR…

### bundle-phase-2b-profile-queries-foundation

**Bundle: Phase 2b (increment 1) — ProfileQueries read port + first migration** — Start of Phase 2b from the [architecture re-audit](../audits/architecture.md#reevaluation--2026-05-29) — the **P2-1** web-layer DB leakage. A survey found **42 raw query occurrences** across ~32 files (`profiles` ×21, `profiles_public` ×16, `friendships` ×5), and they're **heterogeneous**: three…

### bundle-phase-3-inc1-group-aggregate

**Bundle: Phase 3 (increment 1) — Group aggregate + create/update** — Opens Phase 3 of the P2-1 drain: the **groups** subdomain (~28 raw `groups`/`group_members`/`group_followers` hits, no domain model). This first slice stands up the `Group` aggregate + `GroupRepository` and migrates the group **profile** writes (create/update), mirroring the ADR 0020 / UserProfile…

### bundle-phase-3-inc2-group-membership

**Bundle: Phase 3 (increment 2) — group membership + last-owner invariant** — The meaty slice of Phase 3: the `Group` aggregate gains its **membership roster** with the role rules, and finally a home for the one membership invariant nothing enforced before — a group can't lose its last owner.

### bundle-phase-3-inc3-group-follow-edges

**Bundle: Phase 3 (increment 3) — group follow edges** — Continues the groups drain: the `group_followers` follow/unfollow writes move behind `GroupRepository`. Same focused-edge-op shape as the friend edges (inc. 10 / ADR 0020 §5).

### bundle-phase-3-inc4-group-delete

**Bundle: Phase 3 (increment 4) — group delete** — The last group **write**. Folds `deleteGroupAction` onto the aggregate. With this, every group write (create/update, membership, follow, delete) lives behind `GroupRepository` — the groups subdomain's write side is fully drained.

### bundle-phase-3-inc5-group-queries-cards

**Bundle: Phase 3 (increment 5) — GroupQueries read port (cards)** — The groups **write** side is fully behind `GroupRepository` (inc. 1–4); this opens the **read** side with a CQRS read port, mirroring the `ProfileQueries` work from Phase 2b. Group reads are heterogeneous (directory cards, detail + members, my-groups / hostable joins, sitemap, OG), so — like…

### bundle-phase-3-inc6-group-detail-reads

**Bundle: Phase 3 (increment 6) — GroupQueries find-one reads** — Continues the groups read drain (inc. 5 did the card lists). This migrates the four "fetch one group by slug" reads behind the read port.

### bundle-phase-3-inc7-group-roster-reads

**Bundle: Phase 3 (increment 7) — group roster + viewer-role reads** — Continues the groups read drain. Migrates the members roster + the owner/admin gate reads behind `GroupQueries`.

### bundle-phase-3-inc8-group-membership-joins

**Bundle: Phase 3 (increment 8) — group membership joins + sitemap** — The last group reads. Migrates the membership-join reads (my-groups, hostable-groups) and the sitemap slug list behind `GroupQueries`. With this, the groups subdomain (Phase 3) is effectively drained.

### bundle-phase-4-eventrepo-inc1-isp-split

**Phase 4 (EventRepository) inc. 1 — ISP split of the `EventRepository` port (2026-05-29)** — Opens the architecture roadmap's **Phase 4 — split `EventRepository` + decompose the adapter** ([architecture.md](../audits/architecture.md), findings P2-2 / P2-3 / P2-6). This first increment attacks **P2-2** only: the `EventRepository` god-port conflated four responsibilities behind one…

### bundle-phase-4-eventrepo-inc2-getdetail-mappers

**Phase 4 (EventRepository) inc. 2 — `getDetail` decomposition into testable mappers (2026-05-29)** — Second increment of the roadmap's structural Phase 4 (`EventRepository` teardown). This one attacks **P2-3** ([architecture.md](../audits/architecture.md)): `SupabaseEventRepository` was a 1,310-LOC adapter whose `getDetail` alone ran ~480 LOC — ~15 queries plus the inline assembly of the ~80-field…

### bundle-phase-4-eventrepo-inc3-event-detail-cache

**Phase 4 (EventRepository) inc. 3 — event-detail read-path consolidation (P2-6) (2026-05-29)** — Third and final increment of the roadmap's structural Phase 4. Attacks **P2-6** ([architecture.md](../audits/architecture.md)): "one page's data" for `/events/[id]` was spread across three layers — the infra read model (`getDetail`), ~10 `unstable_cache` helpers, and ad-hoc admin reads — inside a…

### bundle-phase-4-inc1-notification-outbox-port

**Bundle: Phase 4 (increment 1) — NotificationOutboxPort (notify fan-out)** — Opens the last big P2-1 subdomain — the notification outbox (Fix #3). This migrates the core fan-out (`lib/notify.ts`, the single entry point every trigger calls) behind a port. Net-new modeling, so it ships with [ADR 0022](../adr/0022-notification-outbox-port.md).

### bundle-phase-4-inc2-outbox-drain-worker-purge

**Bundle: Phase 4 (increment 2) — outbox drain (cron worker + purge)** — Continues the notification subdomain (ADR 0022). inc. 1 did the enqueue side (`notify` fan-out); this does the **drain side** — the cron worker that delivers queued rows and the daily purge.

### bundle-phase-4-inc3-push-subscribe

**Bundle: Phase 4 (increment 3) — push subscribe** — Continues the notification subdomain. inc. 2 put the worker's `push_subscriptions` read/prune behind `PushSubscriptionPort`; this adds the **write** side (the browser subscribe/unsubscribe route).

### bundle-phase-4-inc4-broadcasts

**Bundle: Phase 4 (increment 4) — broadcasts** — Continues the notification subdomain (ADR 0022). Moves the `broadcasts` table behind a `BroadcastPort`.

### bundle-phase-4-inc5-notification-prefs

**Bundle: Phase 4 (increment 5) — notification preferences** — The last named notification surface. Moves the settings page (`profile/notifications`) read + write behind a port — completing the notification subdomain and, with it, all three P2-1 fix items.

### bundle-phase-5-inc1-outbox-uniform-dispatch

**Phase 5 inc. 1 — uniform analytics-outbox dispatch (P2-4) (2026-05-29)** — First increment of the roadmap's **Phase 5** (opportunistic). Closes **P2-4** ([architecture.md](../audits/architecture.md)): the domain-event / analytics outbox was half-wired. Aggregates raised events that no handler delivered — `VolleyballEvent` raises publish/cancel/team/free-agent events and…

### bundle-phase-5-inc2-domain-test-backfill

**Phase 5 inc. 2 — domain test backfill (P3-4) (2026-05-29)** — Second Phase 5 increment (opportunistic). Chips at **P3-4** (thin domain test coverage for newer units — [architecture.md](../audits/architecture.md)). The audit named the priority order: "prioritize `standings` (scoring math) and `community-listing` (claim/approve state machine)." Both were…

## 2026-05-30

### bundle-139

**M3 adoption reality-check + value-preserving shape migration (2026-05-30)** — User asked to "review the M3 alignment and see what items we still need to implement" and to "reevaluate improvements to the strategy." A re-audit of [docs/audits/m3-alignment.md](../audits/m3-alignment.md) against the actual call sites surfaced the headline finding: the 10-bundle arc (129–138)…

### bundle-bridge-view-drop

**Bridge-view drop (Bundle B)** — The migration-only follow-up to [2026-05-30-bundle-bridge-view-retargeting.md](#bundle-bridge-view-retargeting). Bundle A retargeted every call site off the `event_attendees` / `event_free_agents` SECURITY INVOKER views while leaving the views + `INSTEAD OF` triggers in place as a…

### bundle-bridge-view-retargeting

**Bridge-view caller retargeting (Bundle A)** — Closes the `bridge-view callers retargeting` follow-up that's been trailing every event-data-model journal entry since the `event_participants` / `event_participant_payments` collapse (Step 6/7, migration…

### bundle-community-award-voting

**Community award voting — best clip / biggest fail (2026-05-30)** — Follow-up to the event/profile media bundle ([ADR 0024](../adr/0024-event-and-profile-media.md), [journal](#bundle-event-profile-media)), which shipped clips but deferred the "fun award" the user originally asked for. This bundle adds per-event community voting on clips. Scope locked…

### bundle-e2e-phase0-increment-a

**Bundle: E2E Phase 0 increment A — defensive-`catch` sweep** — The e2e coverage audit graded **C7** (reliability remediation incomplete) as the carry-forward of the original 2026-05-25 P1 #1/#2. Two habits were called out:

### bundle-e2e-phase0-increment-b

**Bundle: E2E Phase 0 increment B — helper extraction, withAuthContext, skip-budget guard** — Increment A swept the `.catch(() => false)` probes and confirmed `networkidle` was already gone, but explicitly deferred four items "to a fresh session": the two missing helper modules (`navigation.ts`, `browser.ts`), the skip-budget reporter, and fixing `isVisibleOrTimeout`'s `timeout` arg (a…

### bundle-e2e-phase0-increment-c

**Bundle: E2E Phase 0 increment C — per-worker auth (closes #3, Phase 0 done)** — `fullyParallel: true` + a single shared `user.json` as the authed project's `storageState` meant every parallel worker drove the **same** attendee-a session. Supabase rotates the refresh token on use, so the first worker to refresh invalidated the others' refresh tokens and the rest of the suite…

### bundle-e2e-phase1-brackets

**Bundle: E2E Phase 1 — bracket result-advances-winner + read-only authz (C3)** — C3 (P1): the bracket suite asserted only that the page renders; all six mutations — including **record-result-advances-winner**, the headline risk — were `test.fixme`. Match-result writes go through the captain-vs-host `record_bracket_match_result` RPC (AGENTS pitfall #8) and winner advancement…

### bundle-e2e-phase2-leagues

**Bundle: E2E Phase 2 — league schedule + record + forfeit (C2) (2026-05-30)** — C2 (P1): leagues had **zero** coverage — not even a `test.fixme`. The match-result write goes through the `record_league_match_result` SECURITY INVOKER RPC gated by the `league_schedule_matches_update` RLS policy (host or either captain), exactly the place a silent authorization regression would…

### bundle-event-profile-media

**Event & profile media — Phase 1 (2026-05-30)** — User feature request: video/livestreaming is central to the volleyball community (clips, match VODs, livestreams), but the platform had no media concept. Goal: let users attach **external** videos (we host nothing) to events and profiles, with a per-event livestream list + host-featured stream…

### bundle-league-schedule-ui

**League schedule composition root + host UI** — **Bundle:** Closes the "composition-root wiring", "server actions", and "host UI" follow-ups left open by the previous two league-schedule bundles. Hosts can now manage a per-division weekly slate end-to-end from `/events/[id]/schedule`.

### bundle-league-team-forfeit-ui

**Bundle: League team forfeit host UI** — Follow-up to [`2026-05-30-bundle-p2-7-league-forfeit.md`](#bundle-p2-7-league-forfeit). That bundle landed the `event_team_entries.forfeited_at` column and the types-stub patch but stopped short of any app-layer plumbing — no port, no handler, no panel. This bundle wires the…

### bundle-p1-2-handlers

**League schedule application handlers** — **Bundle:** Follow-up to the P1 #2 thin pass. Adds the CQRS handler layer that sits between the `LeagueSchedule` aggregate and the web app, so the next bundle can focus on server actions + host UI without inventing business rules at the route boundary.

### bundle-p1-2-league-schedule

**Bundle: P1 #2 (league schedule table + LeagueSchedule aggregate)** — **Audit item:** P1 #2 from [docs/audits/event-data-model.md](../audits/event-data-model.md).

### bundle-p2-4-team-composition

**Bundle: P2 #4 TeamComposition vocabulary cleanup** — Closed Step 6 of the event-data-model audit ([docs/audits/event-data-model.md](../audits/event-data-model.md)): renamed the `team_composition` enum value `partner_required` → `partners` everywhere in one bundle. Pre-launch — no production data to backfill, no deprecation window required.

### bundle-p2-7-league-forfeit

**Bundle: P2 #7 league rostered-team lifecycle (`forfeited_at`)** — Closes the last code/schema item in the [event-data-model audit](../audits/event-data-model.md). After this bundle the audit backlog is effectively drained pre-launch — every finding has either a remediation log entry, a documented no-change decision, or a follow-up tracked in a dedicated section.

### bundle-p3-9-aggregate-mirror-fields

**Step 8 / P3 #9 (minimal): drop primary-division mirror fields from the `VolleyballEvent` aggregate** — `VolleyballEvent` was still constructed with `format`, `gender`, and `skillLevel` as top-level fields, even though the corresponding DB columns had already been dropped in…

### bundle-phase-5-inc3-new-event-form-decomposition

**Phase 5 inc. 3 — new-event-form decomposition (P3-1, partial) (2026-05-30)** — Third Phase 5 increment. Chips at **P3-1** (oversized client form — [architecture.md](../audits/architecture.md)): `new-event-form.tsx` was **1,402 LOC** with 21 hook calls, the whole create-event wizard in one file. The form's branch components (`OpenPlayBody`, `PricingSubsection`…

### bundle-phase-5-inc4-stripe-webhook-decomposition

**Phase 5 inc. 4 — Stripe webhook decomposition (P3-2, partial) (2026-05-30)** — Fourth Phase 5 increment. Chips at **P3-2** (Stripe webhook god-handler — [architecture.md](../audits/architecture.md)): the most critical money path had the least structure — an **833-LOC** `api/webhooks/stripe/route.ts` with the signature/idempotency boundary, the dispatch switch, all 7 event…

### bundle-phase-5-inc5-domain-test-backfill

**Phase 5 inc. 5 — domain test backfill, last testable units (P3-4 resolved) (2026-05-30)** — Fifth Phase 5 increment (opportunistic). Closes **P3-4** (thin domain test coverage for newer units — [architecture.md](../audits/architecture.md)). Phase 5 inc. 2 covered the two audit-named priorities (`standings`, `community-listing`) and deferred the rest of the list: `division`…

### bundle-phase-5-inc6-payment-facade-decision

**Phase 5 inc. 6 — payment-facade decision (P3-3 resolved) (2026-05-30)** — Sixth Phase 5 increment. Closes **P3-3** (architecture audit) — "Payment aggregates bypass the application layer (CQRS bypass)." The finding was explicitly a _decide-intentionally_ item: either (a) add `application` command/query handlers for `HostStripeAccount` / `HostSubscription` for consistency…

### bundle-phase-5-inc7-webhook-payment-repo

**Phase 5 inc. 7 — webhook payment writes behind a port (P3-2 resolved) (2026-05-30)** — Seventh Phase 5 increment. Closes the **behavioural half of P3-2** — the half [inc. 4](#bundle-phase-5-inc4-stripe-webhook-decomposition) deliberately deferred. inc. 4 relocated the seven Stripe-webhook handlers out of the 833-LOC `route.ts` into cohesive `lib/webhooks/*` modules…

### bundle-phase-5-inc8-event-form-decomposition

**Phase 5 inc. 8 — event-form decomposition + cross-form DRY (P3-1 resolved) (2026-05-30)** — Eighth (and final-for-the-backlog) Phase 5 increment. Closes **P3-1** — the last open finding from the 2026-05-29 architecture re-audit. inc. 3 had relocated the create-event form's already-parameterized branch/leaf components into `_components/`, taking `new-event-form.tsx` from \*\*1,402 → 698…

### bundle-position-roster-persistence

**Bundle: positional sign-up persistence on event_divisions** — Closes the P3 #9 deferred follow-up that turned out to be more than tidying. While re-reading the audit's "no division column yet" note I noticed the infra repo was reading `events.position_roster` — a column that was dropped in ADR 0006 Phase 9d and never relocated. The save path explicitly…

### bundle-step-5a

**Step 5a — Drop `event_id` from division-scoped tables (2026-05-30)** — P2 #6.5 Bundle B from [docs/audits/event-data-model.md](../audits/event-data-model.md). Bundle A (2026-05-29) already dropped `event_id` from `event_brackets` + `event_team_registrations`. Bundle B is the bigger half — the four remaining division-scoped tables (`event_attendees`, `event_teams`…

### bundle-step-5b

**Step 5b — Collapse team-entry tables (thin pass) (2026-05-30)** — P2 #6.6 from [docs/audits/event-data-model.md](../audits/event-data-model.md). Three tables (`event_teams`, `event_team_registrations`, `event_team_registration_members`) all model "a team registered for a division" with a different `source` discriminator (roster vs. captain-built ad-hoc vs. host…

### bundle-step-5b-ii

**Step 5b.ii — Aggregate / boundary cleanup after team-entry collapse (2026-05-30)** — Follow-up to [Step 5b](#bundle-step-5b). The thin pass kept the aggregate-level `RegistrationSource = Captain | Host | WalkIn` enum in place even though the DB had collapsed `captain` / `host` into a single `ad_hoc` row, and left `attachTeamToDivision` as a select-then-insert because…

### bundle-step-6-7

**Bundle: P2 #6.7 (collapse event_attendees + event_free_agents)** — **Audit item:** P2 #6.7 from [docs/audits/event-data-model.md](../audits/event-data-model.md).

### bundle-walk-in-captain-display-name

**Bundle: walk-in registration column-mapping bug fix** — Closes the P3 follow-up that was carried as "drop unused `captain_display_name` column." Pre-action grep surfaced the opposite: the column is required for every walk-in row by a CHECK constraint, the form captures it, the aggregate enforces it, and the application command threads it through to the…

### formmodal-rsc-client-boundary

**Fix: FormModal callers must be Client Components (2026-05-30)** — Triaging **dev** runtime logs (`develop` → `dev.pickupvb.com` preview) surfaced a single repeating error — 6 occurrences on `GET /events/[id]/bracket`, **intermittently fatal** (one true **500**, plus a `200`-with-error logged in the same second):

### live-match-scoring-phase1

**Live match scoring — Phase 1: promote scoring rules to the domain (2026-05-30)** — User request: let hosts score a scheduled bracket/league match **on the scoreboard**, with the in-progress score reflected live on the public bracket/standings and the result auto-saved — keeping the existing manual entry. Scoped and recorded as [ADR 0023](../adr/0023-live-match-scoring.md)…

### live-match-scoring-phase2

**Live match scoring — Phase 2: `match_live_scores` table + write RPCs (2026-05-30)** — Phase 2 of [ADR 0023](../adr/0023-live-match-scoring.md): the persistence + realtime substrate for the in-progress (live) score, so a Pro host scoring a scheduled match on the scoreboard can have the score surface live on the public bracket/standings. Phase 1 promoted the scoring rules to the…

### live-match-scoring-phase3

**Live match scoring — Phase 3 (pure parts): port, commands, finalize mapping (2026-05-30)** — Phase 3 of [ADR 0023](../adr/0023-live-match-scoring.md): the application/domain glue between the `LiveMatchScore` VO (Phase 1) and the `match_live_scores` persistence (Phase 2). This bundle ships the **layer-pure, fully-verifiable** parts — the repository port, the upsert/clear command handlers…

### live-match-scoring-phase4

**Live match scoring — Phase 4: score on the scoreboard → save to the match (2026-05-30)** — Phase 4 of [ADR 0023](../adr/0023-live-match-scoring.md). Delivers the headline user-requested capability end-to-end: a Pro host launches the live scoreboard pre-seeded for a scheduled match, scores it, and saves the result back to the official bracket/league record — alongside the existing manual…

### live-match-scoring-phase5-readside

**Live match scoring — Phase 5 (read side): public live view (2026-05-30)** — The final piece of [ADR 0023](../adr/0023-live-match-scoring.md): spectators and hosts now see the in-progress scoreboard score render **in place** on the public bracket spectator view, the host bracket page, and the league schedule — with a "LIVE" badge — as a Pro host scores a bound match. The…

### live-match-scoring-phase5-writeside

**Live match scoring — Phase 5 (write side): live persistence adapter + per-point push (2026-05-30)** — Completes the deferred Phase 3 adapter and wires the **write half** of the live-during-play feature (ADR 0023): as a Pro host scores a bound match on the scoreboard, the in-progress score is now persisted (debounced) to `match_live_scores` via the authorization-gated RPC, and cleared when the…

### sponsor-logo-uploads

**Sponsor-logo uploads + hero-walker data-loss fix (2026-05-30)** — The event sponsor slot stored its logo as a free-text `event_sponsors.logo_url` pointing at an arbitrary third-party CDN, rendered through `next/image`. In practice those logos frequently failed to load against **two independent browser walls we can't control per-host**:

### standalone-brackets

**Standalone (event-free) tournament brackets** — Users wanted to run a bracket without hosting an event — a pickup tournament at the gym, a friendly between a few teams — where the full event machinery (geocoded venue, dates, capacity, ticketing, Stripe-host gating, public listings) is pure overhead. The bracket aggregate is one of the richest…

## 2026-05-31

### account-data-export

**Account data-export endpoint — GDPR Art. 20 / CCPA (2026-05-31)** — Follow-on from the [privacy re-audit](../audits/privacy.md) (same day). P3 #12 ("no data-export endpoint") was a standing legal-feature gap: GDPR Art. 20 and CCPA § 1798.100 require a machine-readable export of a user's data on request, and we had neither endpoint nor UI. The audit explicitly…

### account-deletion

**Account deletion — the erasure path (2026-05-31)** — The privacy audit's biggest standing item (P1 #2): no "delete my account" flow, while GDPR Art. 17 requires one. The schema groundwork shipped in Bundle 89 (`profiles.deleted_at` + the FK SET-NULL/CASCADE flips); this bundle builds the application path on top. Follows the same-day data-export…

### admin-community-listing-import

**Admin AI paste-to-listing importer (2026-05-31)** — User request (the platform owner) wanted an easier way to seed the **community listings** directory with volleyball events they personally know about — events that today live as **Facebook events**. Hand-entering each one through `/community/new` is slow, and the create handler's…

### bracket-schedule-cacheable

**Bracket / schedule cacheable spectator pages (2026-05-31)** — The 2026-05-30 performance re-audit opened two items on the public tournament/ league spectator pages ([docs/audits/performance.md](../audits/performance.md)):

### bundle-e2e-standalone-brackets

**E2E: standalone bracket coverage (ADR 0025) (2026-05-31)** — ADR 0025 shipped **standalone (event-free) brackets** — an owner can run a tournament bracket with no event, division, venue, dates, or ticketing. The [standalone-brackets journal](#standalone-brackets) closed with an explicit deferral:

### chat-phase-1-team-room

**Chat messaging — Phase 1 team-room MVP (2026-05-31)** — A chat feature had been started in a session that was lost to a machine restart. The uncommitted work on disk was **Phase 0 + the inner layers of Phase 1**: the schema migration (`20260824000000_chat_messaging.sql`), the `messaging` domain aggregate + ports + tests, and the application…

### chat-phase-2-inbox

**Chat messaging — Phase 2: unread + inbox (2026-05-31)** — Phase 1 (same day) shipped the team-room MVP. Phase 2, per [ADR 0028](../adr/0028-chat-messaging.md), is the **unread / inbox** surface driven by `conversation_participants.last_read_at` vs. `conversations.last_message_at`. The user picked the scope: a `/messages` inbox page **plus** a…

### chat-phase-3-dms

**Chat messaging — Phase 3: direct messages + blocks (2026-05-31)** — Phases 1–2 (same day) shipped the team-room MVP and the inbox/unread surface. Phase 3, per [ADR 0028](../adr/0028-chat-messaging.md), is **1:1 direct messages + the block UI**. The data layer was already complete from Phase 0 — `get_or_create_dm`, `user_blocks`, `is_blocked_pair`, and the DM…

### chat-phase-4-attachments

**Chat messaging — Phase 4: image attachments (2026-05-31)** — The final chat phase per [ADR 0028](../adr/0028-chat-messaging.md): image attachments. Phase 0 had already reserved `messages.attachments jsonb`, pinned empty by a `messages_text_only` CHECK, precisely so this could ship without a table migration. This bundle drops that CHECK, adds the storage…

### chat-retention

**Chat retention — attachment orphan sweep + soft-deleted message scrub (2026-05-31)** — Privacy re-audit finding **#14** (P2): the chat surface (shipped 20260824 messaging, 20260826 attachments) had no retention policy. Two sub-gaps:

### csp-media-embeds

**CSP `frame-src` for media video embeds (2026-05-31)** — User report: YouTube / Twitch embeds on media post feeds (event media sub-page, profile video grids) weren't rendering. Root cause was the enforcing Content-Security-Policy shipped in Bundle 27 (2026-05-22): its `frame-src` directive allowlisted only `challenges.cloudflare.com` and `vercel.live`…

### event-host-manage-dashboard

**Consolidate event host tools into a `/manage` dashboard (2026-05-31)** — User feedback: the way host tools were structured on the event detail page felt cluttered and unclear. An audit of the page confirmed it — host affordances were **8 fragments across 5 visual languages** with no shared home:

### notification-worker-observability-spend

**Notification worker — Vercel observability-event spend (2026-05-31)** — Vercel's monthly budget was being consumed by **observability events**. Root cause was structural, not a traffic spike: the notification outbox worker (`/api/notifications/worker`) was scheduled `* * * * *` — every minute, 24/7 — firing into a mostly-empty queue. ~43,200 function invocations/month…

### pagination-sweep

**Pagination sweep — unbounded UI lists (2026-05-31)** — User asked for an audit of UI list views that render a full result set with no pagination, flagging the `/profile` Hosting section as one example. A read-only sweep found ten list surfaces; six graded P2 (grow monotonically over time or per power-user), the rest P3. This bundle ships the six P2…

### persona-ux-audit

**Persona UX/UI audit + first quick-win bundle (2026-05-31)** — User feedback: "the UI doesn't cater to the different personas" and a request to keep "action items, edit forms, etc." clean and aligned to design standards. Scoped (via clarifying questions) to a **written audit + quick wins**, weighted to the **host**, **visitor→signup**, and **player/attendee**…

### persona-ux-button-sweep

**Persona UX bundle 4: CC-1 button sweep + ratchet (2026-05-31d)** — Fourth bundle off [docs/audits/persona-ux.md](../audits/persona-ux.md), closing **CC-1**: the 59 remaining hand-rolled `bg-primary hover:bg-primary/90 … text-white` primary buttons across 47 files (everything the bundle-1 quick wins didn't already hit). Same migrate-then-ratchet shape as the CC-2…

### persona-ux-field-convergence

**Persona UX bundle 2: field-vocabulary convergence + confirm-dialog (2026-05-31b)** — Second bundle off [docs/audits/persona-ux.md](../audits/persona-ux.md), picking up the P2 the user prioritized: **CC-2** (17 forked local `inputClass`/`labelClass` definitions — the "edit forms look hand-made" half of the original complaint), folding in **CC-4** (the one `window.confirm` that…

### privacy-reaudit-profiles-rls-regression

**Privacy re-audit — owner-only `profiles` RLS regression in chat + media (2026-05-31)** — User asked for a privacy compliance re-audit ("fix any bug and update any suggestions"). The standing audit is [docs/audits/privacy.md](../audits/privacy.md) (2026-05-24), whose P1 #4 step 3 tightened the base `profiles` SELECT policy to owner-only (`auth.uid() = id OR is_platform_admin()`) and…

### seo-reaudit-backlog

**SEO re-audit backlog closed — #5–#10 (2026-05-31)** — The 2026-05-30 SEO re-audit ([docs/audits/seo.md § Reevaluation](../audits/seo.md#reevaluation--2026-05-30)) opened **2 P2 + 4 P3** findings against ~6 months of new public surface (community listings, bracket spectator + league schedule sub-routes, scoreboard tool, stats, legal pages). The…

### third-party-integrations-audit-tier1

**Third-party integrations audit + Tier 1 hardening (2026-05-31)** — Asked for an audit of every external/vendor integration — gaps, bugs, optimizations, and specifically **usage & cost at scale**. No prior `third-party-integrations` audit existed; the surface cross-cuts performance, analytics, monetization, and security audits but the vendor-cost lens is distinct…

### tpi1-3-maptiler-geocoding-tiles

**TPI-1/2/3 — geocoding + map tiles off free OSM, onto MapTiler (2026-05-31)** — Third-party-integrations audit, the last two P2s: address **typeahead** ([autocomplete route](../../apps/web/src/app/api/geocode/autocomplete/route.ts)) hit Photon + Nominatim, and the event map ([event-map.tsx](../../apps/web/src/components/event-map.tsx)) shipped OSM's public tile server. Both…

### tpi6-11-13-p3-bundle

**Third-party integrations P3 bundle — TPI-6, TPI-11, TPI-13 (2026-05-31)** — The last three open findings from the third-party-integrations audit, all P3. With these the audit is fully remediated (only TPI-7's live dev verification remains, and its code already landed).

### tpi7-notification-broadcast

**TPI-7 — notification bell off `postgres_changes`, onto Realtime Broadcast (2026-05-31)** — Third-party-integrations audit **TPI-7** (P2): the site-header notification bell holds a per-tab Supabase Realtime `postgres_changes` subscription on `notifications` — on _every logged-in page_. `postgres_changes` re-evaluates RLS per subscriber per change on one replication stream (the path…

## 2026-05-28 … 2026-05-29 — Bracket `entry_id` cutover

> These six bundles were authored 2026-05-28/29 (per git) but their filenames carried a mislabeled `2026-12-04` date; folded here with the rest of May.

### bundle-bracket-matches-entry-id-groundwork

**Bundle: Bracket matches `entry_*_id` groundwork** — Schema-only groundwork closing the pre-work half of the carry-over "bracket-reader `source='roster'` filter loosening" follow-up on the [event data model audit](../audits/event-data-model.md). The application half (read path, write path, `BracketTeamLite.entryId`) is deliberately deferred to…

### bundle-bracket-seeds-entry-id-groundwork

**Bundle: Bracket seeds `entry_id` groundwork** — Schema-only companion to the [bracket matches `entry_*_id` groundwork bundle](#bundle-bracket-matches-entry-id-groundwork) shipped earlier today. Lands the polymorphic pair on `bracket_seeds` so the future bracket-reader filter-loosening bundle can flip seeds and matches together…

### bundle-bracket-reader-entry-id-projection

**Bundle: Bracket-reader `entryId` projection + filter swap** — First slice of the application half of the carry-over "bracket-reader `source='roster'` filter loosening" follow-up on the [event data model audit](../audits/event-data-model.md). The two schema-groundwork bundles shipped earlier today…

### bundle-bracket-write-path-cutover

**Bracket matches + seeds + work-team write-path cutover** — Closes the application half of the carry-over `event-data-model.md` filter-loosening line. After the three groundwork migrations (entry columns on `bracket_matches` + `bracket_seeds`, and the read-side filter/projection flip in the bracket-reader bundle) the bracket write path was the last consumer…

### bundle-event-team-registration-forfeited-at

**Bundle: `EventTeamRegistration.forfeitedAt` aggregate mirror** — Follow-up slice on the event data model audit, closing the carry-over "`EventTeamRegistration.forfeitedAt` wiring" follow-up from [the league-team-forfeit-ui bundle](#bundle-league-team-forfeit-ui). That bundle shipped the host-facing affordance via the…

### bundle-captain-rls-match-result

**Captain-RLS on match-result writes** — Closes the last carry-forward from [event-data-model.md](../audits/event-data-model.md) — the "captain-RLS gap on bracket + league match-result writes" flagged in the `save_league_schedule` and `save_bracket` RPC scope notes. It is an authorization concern, so it's also recorded as a follow-on to…
