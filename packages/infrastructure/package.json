{
  "name": "@pickupvb/infrastructure",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@pickupvb/domain": "workspace:*",
    "@pickupvb/supabase": "workspace:*"
  },
  "devDependencies": {
    "@pickupvb/config": "workspace:*",
    "@types/node": "25",
    "typescript": "^6.0.3"
  }
}
