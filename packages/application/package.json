{
  "name": "@pickupvb/application",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'lint: no rules yet' && exit 0",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@pickupvb/domain": "workspace:*",
    "@pickupvb/types": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "25",
    "typescript": "^6.0.3"
  }
}
